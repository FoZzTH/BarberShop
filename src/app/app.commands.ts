export const startCommand = /^\/start$/;
export const helpCommand = /^\/help$/;
export const viewCommand = /^\/view$/;
export const procCommand = /^\/proc (.+)$/;

export const listCommand = /^\/list$/;
export const oldCommand = /^\/old$/;
export const abortCommand = /^\/abort$/;
